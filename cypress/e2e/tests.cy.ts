describe('template spec', () => {
  it('passes', () => {
    //cy.get(id, un comoposant html).should("exist").should("not.exists")
    //cy.contains("back-button").click()
    //cy.contains("form").type("uzaubzec")

    cy.visit('https://example.cypress.io')
  })
})